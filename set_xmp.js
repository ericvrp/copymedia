// set items in the .xmp file of media based on the users previous input

//
const stars = copyItem => {
    // console.warn(`TODO: set_xmp.stars`);
} // end of stars(...)

//
const color = copyItem => {
    // console.warn(`TODO: set_xmp.color`);
} // end of color(...)

//
const accept_reject = copyItem => {
    // console.warn(`TODO: set_xmp.accept_reject`);
} // end of accept_reject(...)

//
module.exports = {
    stars,
    color,
    accept_reject,
}
